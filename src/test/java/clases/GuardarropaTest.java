package clases;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertTrue;

import java.util.List;

import org.junit.Before;
import org.junit.Test;

public class GuardarropaTest {

	Guardarropa guardarropa;
	Sugerencia sugerencia;
	/*
	@Before
	public void prepararClases() {
		guardarropa = new Guardarropa();
		sugerencia = new Sugerencia();
		sugerencia.setTemporada("verano");

	}

	@Test
	public void elGuardarropaEstaVacio() {
		assertEquals(0, guardarropa.getSugerencias().size());
	}

	@Test
	public void elGuardarropaTieneRopa() {

		guardarropa.agregarSugerencia(sugerencia);
		assertTrue(guardarropa.getSugerencias().size() > 0);
	}

	@Test
	public void tengoRopaParaVerano() {

		guardarropa.agregarSugerencia(sugerencia);
		List<Sugerencia> lista = guardarropa.sugerime("verano");
		assertTrue(lista.size() > 0);

	}
	*/

}